@AGENTS.md
@docs/quick-filter-spec.md
@docs/보배드림_차량이미지_로고_에셋지침_v1.md
퀵필터 작업 보고는 reports/work-log.csv, 채팅 답변은 "브랜치 / 커밋 해시 / 상태"만.

배포 보고 형식(배포할 때마다):
- v 값 = 배포된 main 커밋 해시 7자리
- 모바일: https://bobaekimboae.github.io/bobaedream/?qf=guazi&v=<v값>
- PC: https://bobaekimboae.github.io/bobaedream/?qf=guazi&v=<v값>&pc=1
- 배포 확인: `&debug=1` 을 붙이면 오른쪽 아래에 "브랜치 @ 커밋 · 빌드 시각"이 보인다(OP-011). 이 값이 새 커밋이어야 배포 완료
- 번들 파일명(dist/client/assets/index-*.js, 배포본 HTML에서 확인)
- 초톳·동처띠 모드를 바꾼 작업이면 해당 모드 링크도(qf=chotot / qf=dongchedi)

## 작업 보고
- 작업 중에 쓰는 모든 설명·진행 메시지·최종 보고는 한국어로 쓴다. 코드·파일명·명령어·브랜치명은 원래 표기 그대로 둔다.
- 쉬운 말로 쓴다. 기술 용어는 처음 나올 때 짧게 풀어쓴다(예: "computed style(화면에 실제 적용된 스타일 값)").
- 단계가 바뀔 때마다 맨 앞에 한 줄 진행 표시: `[현재 단계/전체 단계 · 약 N%] 지금 하는 일`. 보고가 아니므로 멈추지 않는다.
- 자동 대조를 반복할 때는 한 바퀴마다 영역별 차이 비율을 한 줄로(예: `[9/10 · 반복 2회차] PC 상단 패널 43% → 12%`). 사람 확인을 기다리지 않고, 최종 보고는 끝에 한 번.
- 작업이 끝날 때마다 채팅 보고와 같은 내용을 보고서 파일로도 남긴다. 위치: `reports/pr-<PR 번호>.md`(PR이 없으면 `reports/<브랜치명>.md`). 한국어, 순서: ① 한 줄 요약 ② 과제 ID · 브랜치 · 커밋 · PR 링크 · 상태(푸시/병합·배포 v값) · 이번 병합으로 main에 함께 들어간 과제 ③ 한 일(쉬운 말로 3~7줄) ④ 원본과 비교(영역별 차이 비율 표 작업 전 → 후, 동작 점검표가 있으면 함께) ⑤ 검증(verify:qf 결과, 콘솔 오류 PC·모바일, measure:qf 퀵필터 규격 레일 96·카드 80×72·이미지 56×28·트림 칩 32, 개발 시안형 필터 선택지 303개·시트 마감 6·광고기간 "전체" 유지) ⑥ 원본과 다르게 남긴 것과 이유 ⑦ 못 한 것·다음에 할 것 ⑧ 확인 링크(모바일 `?qf=guazi`, PC `?qf=guazi&pc=1` — category 없이, 배포본은 `&v=<커밋>`).
- 보고서 캡처: 보고에 쓴 원본·우리 나란히 캡처만 `reports/pr-<번호>/`에 PNG로 저장해 커밋한다(장당 500KB 이하로 줄여서, 최대 8장). 보고서 안에서는 상대 경로로 넣는다. 나머지 작업용 캡처(`reports/shots/`, `reports/diff/`)는 커밋하지 않는다.
